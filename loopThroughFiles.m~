% Path passed to function MUST end with a /
%
function [fileList] = listFiles(path,sequence)
    
    
    if path(end) ~= '/'
        path = [path '/'];
    end
    
    folderStruct = dir(path);
    folders = struct2cell(folderStruct);
    folders = folders(1,3:end-1);
    
    for fold = folder s
        dir([pat
